# Skill 调整建议：权限、弹窗与提交

## 目标

减少无必要的权限弹窗，同时保持每个 task 完成后必须提交的工作流约束。

## 核心区分

“每个 task 必须提交”是 local-workflow 的生命周期要求，不等于“每次提交都必须请求提权”。

权限请求只由运行环境决定：当命令超出当前沙箱或既有授权规则时才请求。skill 不应主动把普通
提交、测试或临时目录验证描述成必须弹窗的操作。

## 权限分级

### 默认直接执行

- 读取仓库文件和 Git 元数据；
- 工作区内的普通文件修改；
- `/tmp` 下固定临时目录的验证文件；
- 已有授权规则覆盖的 `git add`、`git commit`；
- 编译、测试、归档和静态检查。

### 仅在环境拒绝时请求权限

- 写入受限目录；
- 使用未授权的命令前缀；
- 需要网络、外部服务或远端 Git 操作；
- 平台明确判定为高风险的命令。

不要因为“这是提交”或“这是测试”而主动请求提权。

## 执行规则

1. 每个 task 仍由 dispatcher 负责暂存、提交和记录 commit；这是强制工作流，不依赖弹窗。
2. 验证统一使用固定临时目录，例如 `/tmp/<project>-verify-task-N/`，一次批量完成安装、冲突、
   覆盖、归档和清理检查。
3. 不使用覆盖范围不明确的 `bash -c`、`rm -rf /tmp` 或全局目录清理；只操作当前 task 创建的
   明确文件和目录。
4. worker contract 必须声明：
   - Allowed read paths；
   - Forbidden read paths；
   - Allowed read-only commands；
   - Forbidden mechanical commands。
5. 状态文件记录命令、结果、生成物和清理结果，不把权限弹窗作为 task 完成条件。
6. 如果验证确实被权限阻断，先缩小命令和路径范围；仍无法执行时，生成一个只写固定 `/tmp`
   目录的验证脚本交给用户运行，返回结果后继续审查和提交。

## 提交规则

每个 task 完成后必须：

1. 检查测试和 diff；
2. 只暂存 task-owned paths；
3. 执行一次对应的 task commit；
4. 将 commit、验证证据和审查结果写入 workflow ledger。

只有工具实际返回权限错误时才重新请求授权。不要为了满足“必须提交”而额外制造授权弹窗。

## 推荐写入 local-workflow 的规范文字

> 每个 task 完成后必须由 dispatcher 暂存并提交。提交是工作流要求，不意味着每次提交都需要
> 权限弹窗。普通工作区修改、固定 `/tmp` 验证以及已获授权的 Git 命令直接执行；只有运行环境
> 明确拒绝当前命令时才请求提升权限。验证应批量执行，并限制在 task 指定路径和固定临时目录内。
